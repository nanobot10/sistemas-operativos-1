syntax = "proto3";

message Number {
    string nombre = 1;
    string departamento = 2;
    int32 edad = 3;
    string formaDeContagio = 4;
    string estado = 5;
}

service Calculator {
    rpc SquareRoot(Number) returns (Number) {}
}